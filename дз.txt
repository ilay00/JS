	a)	if (!("a" in window)) {
		var a = 1;
		}                     // результат этого кодо буднт undefined так как условия отичается от выполнения операции
		alert(a);

		var b = function a(x) {
	b)			x && a(--x);
				};
				alert(a);           // результат  undefined так как переменная обявлина b функция имеит вид a(); итог в alert отоброжаит просто a

    c)    function a(x) {
		return x * 2;
		}
		var a;
		alert(a);                  // результат null так как имеит вид a(); переменная обявлина но ей не присвоенно значения


	 d)             
	                function b(x, y, a) {
					arguments[2] = 10;
					alert(a);              //результат будет 3 так как в функции b() описон только третий пораметор вывод через alert на экран    
					}
					b(1, 2, 3);


	e)                  function a() {
						alert(this);
						}
						a.call(null);			// результат скорее null так как  на выходе имеит this а метод call присваивает перваму порамитру this

